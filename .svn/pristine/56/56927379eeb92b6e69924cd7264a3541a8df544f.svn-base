
import network from '@src/network/index';
const preUrlPath = '/right';

export default {
    async list () {
        const res = await network.get({
            url: `${preUrlPath}/list`
        });
        return res;
    }
}
